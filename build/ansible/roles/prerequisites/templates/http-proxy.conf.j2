[Service]
{% if http_proxy is defined and http_proxy|length %}
Environment="HTTP_PROXY={{ http_proxy }}"
{% endif %}
{% if https_proxy is defined and https_proxy|length %}
Environment="HTTPS_PROXY={{ https_proxy }}"
{% endif %}
{% if no_proxy is defined and no_proxy|length %}
Environment="NO_PROXY={{ ansible_host }},{{ no_proxy }}"
{% endif %}