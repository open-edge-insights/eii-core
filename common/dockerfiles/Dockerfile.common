ARG UBUNTU_IMAGE_VERSION
ARG EIS_VERSION

FROM ubuntu:$UBUNTU_IMAGE_VERSION

# Setting ENV for pybase containers
ENV PY_WORK_DIR /EIS

ENV LD_LIBRARY_PATH $LD_LIBRARY_PATH:"/usr/local/lib"

COPY libs ./libs
COPY util/ ./util
